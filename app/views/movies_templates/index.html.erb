<h2>Hey, Gehna! What are we watching today?</h2>

<form action="/" method="get">
  <label for="min_rating_field">Min rating (0–10)</label>
  <input
    id="min_rating_field"
    type="number"
    name="min_rating"
    value="<%= params[:min_rating] %>"
  >

  <label for="max_runtime_field">Max runtime (minutes)</label>
  <input
    id="max_runtime_field"
    type="number"
    name="max_runtime"
    value="<%= params[:max_runtime] %>"
  >

  <button>Find movies</button>
  <a href="/">Clear filters</a>
</form>

<h2>Results</h2>

<p>Params: <%= params.inspect %></p> <!-- temporary debug so you can see filters -->

<p>Found <%= @movies.size %> movies.</p>

<ul>
  <% @movies.each do |movie| %>
    <li>
      <strong><%= movie["title"] %></strong>
      – ⭐ <%= movie["vote_average"] %>
      – <%= movie["release_date"] %>
    </li>
  <% end %>
</ul>
